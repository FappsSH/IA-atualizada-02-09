# Project Instructions

For this project, always use the `caveman` skill by default during the entire conversation unless the user explicitly asks for a different style or a task requires another mandated skill.

Operational rule:
- Treat `/caveman` as the default communication mode for all work in this repository.
- Keep responses concise and token-efficient.
- If another skill is explicitly requested or clearly required by task rules, use it together with `caveman` when possible.
