import { FollowupsClient } from './FollowupsClient';

export default function FollowupsPage() {
  return (
    <FollowupsClient
      initialConfigs={[]}
      initialPendentes={[]}
      initialHistorico={[]}
    />
  );
}
